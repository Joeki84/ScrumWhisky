{% extends 'base_with_right_sidebar.html.twig' %}

{% block title %}The Scotch Lodge{% endblock title %}


  {% block main_column %}

    <nav id="topmenu" class="clearboth">
    <!-- OLD BOOKMARK
      <div id="bookmark">
        <div>
        </div>
      </div>
    -->
      <ul class="nav nav-tabs">
        <li><a href="{{ app.urlFor('main_page') }}">Home</a></li>
        <li><a href="#"> Tab 2 </a></li>
        <li><a href="#"> Tab 3 </a></li>
        <li><a href="#"> Tab 4 </a></li>
      </ul>
    </nav>
    
    <div class="container welcome">
      {% if globals.user is defined and globals.user is not null %}
      {% set user = globals.user %}
        <h1>Have a drink, {{ globals.user.username }}</h1>
      {% if user.isAdmin() %}
        <span class="status">Logged in as admin</span>
      {% else %}
        <span class="status">Logged in as user</span>
      {% endif %}
      {% else %}
        <h1>Welcome</h1>
      {% endif %}
    </div>
    <div id="event_1">
      <div class="container">
    {% for event in events_one %}
        <h3>Be sure to check out our next event on {{ event.EventDate|date('d-m') }} @ {{ event.postcode.town }}</h3>
    {% endfor %}
      </div>
    </div>

     <a href="{{ globals.app.urlFor('advanced_search_whisky') }}">Whisky advanced search </a>
    
  {% endblock main_column %}

  {% block sidebar %}    
      {% include 'menu.html.twig' %}
      <div id="event_5">
        <h3>Upcoming events:</h3>
        <dl>
        {% for event in events_five %}
          <dt>{{ event.Title }}</dt> 
          <dd>{{ event.EventDate|date('d-m-Y') }}</dd>        
        {% endfor %}  
        </ul>
      </div>
        
      {% include 'Comments/latest_comments.html.twig' %}
      {% include 'Comments/popular_comments.html.twig' %}
      {% include 'Whisky/latest_reviews.html.twig' %}
      {% include 'Whisky/popular_reviews.html.twig' %}
  {% endblock sidebar %}


{% block extrajs %}
  <script src="js/main.js"></script>
{% endblock extrajs %}
