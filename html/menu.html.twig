

  {% set app = globals.app %}

  {% if globals.user is not defined or globals.user is null %}
    <nav id="reg-menu">
      <ul class="nav nav-pills floatright">
        <li>
          {% block login %}
            {% include 'Profile/logon.html.twig' %}
          {% endblock login %}
        </li>
        <li><a href="{{ app.urlFor('user_register') }}">Register</a></li>
        <li><a href="{{ app.urlFor('password_reset_request') }}">Forgot password</a></li>
      </ul>
    </nav>
  {% else %}
    <nav id="reg-menu">
      <ul class="nav nav-pills floatright">
        <li><a href="{{ app.urlFor('profile_show') }}" class="btn">Profile</a></li>       
        <li><a href="{{ app.urlFor('user_logoff') }}" class="btn">Logoff</a></li>
      </ul>
    </nav>
  {% endif %}
